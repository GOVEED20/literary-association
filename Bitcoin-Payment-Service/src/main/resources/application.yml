server:
  port: 0
  ssl:
    enabled: true
    key-store-type: PKCS12
    key-store: Bitcoin-Payment-Service/src/main/resources/bitcoinService.p12
    key-alias: bitcoinService
    key-store-password: goveda2020
    # client-auth: need
    trust-store: Bitcoin-Payment-Service/src/main/resources/bitcoinServiceTruststore.jks
    trust-store-password: goveda2020
    trust-store-type: JKS

spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: ENC(DJV5MsDLF/6adraw4pj3ZQ==)
    url: jdbc:h2:file:./bs-db
    username: dzabic
    initialization-mode: never
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jasypt:
  encryptor:
    password: goveed20
